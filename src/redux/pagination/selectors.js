export const selectVisibleCount = (state) => state.pagination.visibleCount;
export const selectPage = (state) => state.pagination.page;
export const selectPerPage = (state) => state.pagination.perPage;
