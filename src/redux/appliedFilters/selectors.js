export const selectLocation = (state) => state.appliedFilters.location;
export const selectType = (state) => state.appliedFilters.type;
export const selectFeatures = (state) => state.appliedFilters.features;
